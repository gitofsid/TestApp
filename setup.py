from setuptools import setup

setup(
    name = "Organ Donation",
    version = "1.0",
    description = "organ donation app",
    packages = ["frontend"],
    package_dir={'models': 'frontend/models'}
)
